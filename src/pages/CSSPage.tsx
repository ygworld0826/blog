import React from 'react';

const CSSPage: React.FC = () => {
  return (
    <div>
      <h2>CSS 정리</h2>
      <p>CSS (Cascading Style Sheets)는 웹 페이지의 스타일을 정의합니다.</p>
      <ul>
        <li>선택자와 특이성</li>
        <li>레이아웃 (Flexbox, Grid)</li>
        <li>반응형 디자인</li>
      </ul>
    </div>
  );
};

export default CSSPage;
