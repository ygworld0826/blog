import React from 'react';

const HTMLPage: React.FC = () => {
  return (
    <div>
      <h2>HTML 정리</h2>
      <p>HTML (HyperText Markup Language)은 웹 페이지를 구성하는 기본 언어입니다.</p>
      <ul>
        <li>태그와 요소</li>
        <li>시맨틱 태그</li>
        <li>폼과 입력</li>
      </ul>
    </div>
  );
};

export default HTMLPage;
